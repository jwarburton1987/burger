<h1>Eat-Da-Burgers</h1>

<ul>
 {{#each Burgers}}
   {{#unless devoured}}
     {{> Burgers/Burgers-block sleep=true}}
   {{/unless}}
 {{/each}}
</ul>
<ul>
 {{#each Burgers}}
   {{#if devoured}}
     {{> Burgers/Burgers-block sleep=false}}
   {{/if}}
 {{/each}}
</ul>
<form class="create-form">

 <div class="form-group">
   <label for="ca">Burger Name:</label>
   <input type="text" id="ca" name="burger_name">
 </div>

 {{!-- <div class="form-group">
   <label for="devoured">devoured?</label><br>
   <input type="radio" name="devoured" value="1" checked> Yummmm!<br>
   <input type="radio" name="devoured" value="0"> Ewwww
 </div> --}}

 <button type="submit">Submit</button>
</form>
Enter a burger you want to eat,click the submit button!